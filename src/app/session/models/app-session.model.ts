export interface AppSession {
  bonuses: number[];
  currentPace: number;
  currentStart: number;
  deaths: number[];
  levelScores: number[];
  nonStartLevelCount: number;
  shorthandMultiplier: number;
}
