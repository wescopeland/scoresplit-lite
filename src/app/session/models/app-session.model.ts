export interface AppSession {
  bonuses: number[];
  currentPace: number;
  currentStart: number;
  deaths: number[];
  levelScores: number[];
  shorthandMultiplier: number;
  subtractionCache: number;
}
